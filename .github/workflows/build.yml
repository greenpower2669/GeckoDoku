name: Build GeckoDoku APK + AAB

on:
  push:
    branches: [ main, gecko-022-rich-media ]
  workflow_dispatch:

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.0'
      - name: Test and build Android
        run: gradle :app:testDebugUnitTest :app:assembleDebug :app:bundleDebug --no-daemon
      - name: Name APK
        run: cp app/build/outputs/apk/debug/app-debug.apk GeckoDoku-v0.9.0-dev.apk
      - name: Name AAB
        run: cp app/build/outputs/bundle/debug/app-debug.aab GeckoDoku-v0.9.0-dev.aab
      - uses: actions/upload-artifact@v4
        with:
          name: GeckoDoku-v0.9.0-dev-Android
          path: |
            GeckoDoku-v0.9.0-dev.apk
            GeckoDoku-v0.9.0-dev.aab
